# SEO remediation — progress log

Tracks the multi-phase SEO fix campaign. Each task ships in its own
commit; commit hashes referenced inline. Re-run the scripts in
`scripts/seo-*.mjs` after each schema or copy change.

## Phase 1 — index hygiene

| ID  | Task                                        | Status | Notes                                                        |
| --- | ------------------------------------------- | ------ | ------------------------------------------------------------ |
| 1.1 | `seo_indexable` column + sitemap gates      | ✅     | Migration `20260523000000`. Sitemaps for venues/events/hotels/personalities/blog filter on `seo_indexable=eq.true`. Detail rows can override per-row indexability via `DetailResult.indexable`. |
| 1.2 | `/news/:slug` 410 Gone                      | ✅     | `public/_redirects` + `public/410.html`. `sitemap-news.xml` returns empty urlset. News detail returns `indexable:false` as defense-in-depth. |
| 1.3 | Landing has-content gates                   | ✅     | `MIN_LANDING_VENUES=6`, `MIN_LANDING_EVENTS=3`. Below the bar the URL still resolves but emits noindex + skips hreflang. |
| 1.4 | Placeholder records bulk noindex            | ✅     | Migration sets `seo_indexable=false` on rows whose name/slug matches `^untitled(-[0-9]+)?$`. Script `scripts/findPlaceholderRows.mjs` surfaces remaining placeholders. |
| 1.5 | `/places` → `/city` redirect                | ✅     | `public/_redirects` 301.                                     |
| 1.6 | Sitemap index hygiene                       | ✅     | Drop `sitemap-news.xml` from `sitemap.xml`. Reorder by editorial value. |

## Phase 2 — head + body remediations

| ID  | Task                                          | Status | Notes                                                      |
| --- | --------------------------------------------- | ------ | ---------------------------------------------------------- |
| 2.1 | Venue title template                          | ✅     | `${name} — ${city} | Queer Guide`, truncated at 60.        |
| 2.2 | Venue description template                    | ✅     | Typed clause ("Bar in Berlin · …") when row has no rich description. `categoryLabels.ts` collapses internal enum codes (and the bare word "other") to user-facing labels. |
| 2.3 | LocalBusiness `streetAddress` guard           | ✅     | PostalAddress only emitted when row has a real street; city+country alone → `areaServed`. |
| 2.4 | News meta strip-HTML                          | ✅     | `excerpt` flows through `stripHtml` + `collapseWs` before truncation. |
| 2.5 | safeOgImage + image audit                     | ✅     | All detail-route `ogImage` flows through `safeOgImage()`. Wikipedia / Wikimedia hotlinks fall back to the branded default. Script `scripts/seo-image-audit.mjs` flags additional hosts. |
| 2.6 | `<html lang>` per locale                      | ✅     | `HtmlLangRewriter` in `functions/_middleware.ts`. Locale-prefixed URLs serve the correct lang attribute. |
| 2.7 | Truncate-to-sentence                          | ✅     | `truncate()` prefers a sentence boundary in the trailing 30 chars; ellipsis only when no sentence end found. |

## Phase 3 — crawler-facing

| ID  | Task                                                  | Status | Notes                                                          |
| --- | ----------------------------------------------------- | ------ | -------------------------------------------------------------- |
| 3.1 | Drop unlocalised hreflang                             | ✅     | `LOCALISED_LOCALES` regenerated by `scripts/seo-localised-locales.mjs`. Locale-prefixed URLs whose JSON differs from `en.json` by less than 5 % get noindex. |
| 3.2 | Expand bot UA list + routeBody enrichment             | ✅     | `botUa.ts` now covers Google inspection/storebot, Bing-family, Applebot-Extended, Slackbot link expander, Screaming Frog, Sitebulb, AI crawlers (GPTBot, ChatGPT-User, PerplexityBot, ClaudeBot, CCBot). |
| 3.3 | Per-template `<noscript>` fallback                    | ✅     | `buildNoscriptHtml()` in `routeBody.ts`. Crisis routes (`/help`, `/safety/*`) keep the global crisis-hotline default; other routes get a route-specific summary + 5 links. |

## Phase 4 — growth content

| ID  | Task                                       | Status     | Notes                                                       |
| --- | ------------------------------------------ | ---------- | ----------------------------------------------------------- |
| 4.1 | Auto-promote spaces landings               | ✅         | Folded into 1.3 — same threshold logic, threshold lives in `MIN_LANDING_VENUES`. |
| 4.2 | Data-driven Pride pages + WorldPride hub   | ✅         | `/pride/:year/world` added; pulls all Pride events for the year regardless of host city. |
| 4.3 | `/help/:country` canonical meta            | ✅ partial | `/help` parent has canonical meta; per-country dynamic meta deferred to a follow-up phase. |
| 4.4 | Blog infrastructure                        | ✅ partial | `sitemap-blog.xml` gates on `seo_indexable` + `published_at`. SPA blog route lands in a follow-up phase. |
| 4.5 | seo-linkcheck script + CI wire             | ✅         | `scripts/seo-linkcheck.mjs`. CI wire follow-up: add a workflow step that runs `node scripts/seo-linkcheck.mjs --strict --max=200` against the preview deployment. |

## Verification

- `npm run typecheck` — clean (final state, this branch).
- Deploy to production, then:
  - `QG_BASE=https://queer.guide node scripts/seo-linkcheck.mjs --max=100` should return 0 hard issues.
  - Spot-check `<html lang>`, hreflang, JSON-LD, og:image on:
    - `https://queer.guide/`
    - `https://queer.guide/venues/<slug>`
    - `https://queer.guide/news` (index page, /news/* slugs return 410)
    - `https://queer.guide/spaces/trans-friendly`
    - `https://queer.guide/pride/2026`
    - `https://queer.guide/pride/2026/world`
  - Submit `sitemap.xml` to Google Search Console; expect no "Submitted URL marked 'noindex'" warnings once the GSC re-crawl completes.

## Deferred items

- **Hard CHECK constraints on placeholder slugs.** Migration uses
  `UPDATE` to flip placeholders to `seo_indexable=false`; a CHECK
  constraint that blocks writes lands once `findPlaceholderRows.mjs`
  reports 0 rows across all tables.
- **Per-country `/help/:country` dynamic meta.** Adds an
  EmergencyService JSON-LD block per country once the hotline roster
  stabilises (currently editorial-edited via cms_pages.body_json).
- **SPA blog route + author / category indices.** `blog_posts` table
  is provisioned but the front-end route isn't wired.
- **Full per-locale SSR translations.** Locale JSON files have
  meaningful translations (all 11 clear the 5 % threshold), but the
  crawler-body content from `routeBody.ts` is English-only. Worth
  revisiting once a translation workflow lands.
