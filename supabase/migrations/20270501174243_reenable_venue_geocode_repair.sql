-- NO-OP TOMBSTONE. The work that was here now lives in
-- 20270602094137_reenable_venue_geocode_repair.sql. Do not restore it here.
--
-- WHY THE CONTENT MOVED, AND WHY THIS FILE STILL EXISTS
--
-- As written, this migration deadlocked `supabase db push` for the whole repo
-- from 2026-09-05 08:46Z. Eight merged migrations never applied while edge
-- functions kept deploying, so prod ran new code against the old schema:
--
--   ERROR: sync_automations_to_cron(true) unscheduled jobs as a side effect of
--   re-enabling venue_geocode_repair: ["marketplace_image_mirror"] (P0001)
--
-- NEITHER MIGRATION INVOLVED IS WRONG. The ORDER was, and it was a MUTUAL
-- deadlock, which is why fixing one side alone does not clear it:
--
--   1. This migration calls sync_automations_to_cron(true), whose branch (b)
--      unschedules every DISABLED registry row. `marketplace_image_mirror` was
--      disabled — falsely auto-paused; 20270601100100 measures it at 3,514 runs
--      / 25 errors (0.7%), every failure a correlated DB-side statement timeout
--      — but still carried a live cron.job. So the sync killed it, and the guard
--      refused. That guard is correct and is preserved verbatim in the new file:
--      a registry row disabled without its cron retired is exactly the fault it
--      exists to catch.
--
--   2. The migration that REPAIRS that row is 20270601100100, which sorted
--      LATER and so could never run. It also asserts `venue_geocode_repair`
--      stays disabled (its `k_stay_off` list) — so even past (1), this file
--      running first would have aborted 20270601100100 in turn.
--
-- Ordering the real work after 20270601100100 satisfies both: the restore
-- re-enables marketplace_image_mirror first, so the sync has nothing to kill;
-- and venue_geocode_repair is still disabled when the restore asserts it.
-- No guard was relaxed and no logic changed — only when it runs.
--
-- WHY A TOMBSTONE RATHER THAN A RENAME. Renaming the file outright is what the
-- pre-push drift hook blocks, and it is right to: it cannot read remote history
-- (no SUPABASE_ACCESS_TOKEN), so it treats every merged version as applied. Here
-- that inference is false — `db push` aborted INSIDE this migration's
-- transaction on three consecutive deploy runs (10:17, 10:31, 11:39Z), each of
-- which re-attempted it, and db push skips versions it has already applied — but
-- "provably unapplied" is not something the hook can check, and disabling a
-- drift guard to move a migration is not a trade worth making. Keeping the
-- version present costs one no-op and keeps the guard armed for everyone else.

do $$ begin end $$;
