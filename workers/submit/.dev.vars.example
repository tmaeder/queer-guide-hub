# Local secrets for `wrangler dev`. Copy to `.dev.vars` and fill in. Never commit `.dev.vars`.
SUPABASE_URL=https://xqeacpakadqfxjxjcewc.supabase.co
SUPABASE_ANON_KEY=     # publishable anon key (apikey header to PostgREST)
SUPABASE_JWT_SECRET=   # HS256 secret used by Supabase Auth to verify user JWTs
