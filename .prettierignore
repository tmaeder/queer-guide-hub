dist
node_modules
supabase/functions
public
*.min.js
*.min.css
package-lock.json

# Generated by `supabase gen types` — must stay byte-identical to the
# generator output. lint-staged's prettier pass rewrote it with semicolons,
# which broke the fieldColumnTypes guard that parses this file as the schema
# of record (and produced a 36k-line diff for nothing).
src/integrations/supabase/types.ts

# Kept byte-identical to supabase/functions/_shared/entity-classifier.ts (see
# that file's header). supabase/functions is prettier-ignored above, so the Deno
# copy is written in Deno style; running prettier here rewrites this one with
# semicolons and silently breaks the twin. lint-staged's prettier pass did
# exactly that on the way in — same failure mode as the types.ts entry above.
src/lib/entityClassifier.ts
