import { SavedTab } from '@/components/profile/tabs/SavedTab';

/** Hub saved module — the unified favorites surface, ported from /me/saved. */
export function SavedModule() {
  return <SavedTab />;
}
