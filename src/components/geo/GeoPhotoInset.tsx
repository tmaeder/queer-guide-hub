/** Moved to `@/components/transit/PhotoInset` — the venue and event singles
 *  need the same frame. Re-exported so the geo pages did not have to change. */
export { PhotoInset as GeoPhotoInset } from '@/components/transit/PhotoInset';
