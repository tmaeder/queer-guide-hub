/** See `GeoSections.tsx` — the model moved to `@/components/transit`. */
export {
  singleSections as geoSections,
  singleStations as geoStations,
  useSingleActiveSection as useGeoActiveSection,
  type SingleSectionDef as GeoSection,
} from '@/components/transit/singleSectionModel';
