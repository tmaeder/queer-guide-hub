interface SparklesProps {
  density?: number;
  className?: string;
  minSize?: number;
  maxSize?: number;
  speed?: number;
}

/**
 * Decorative sparkles background — gutted to no-op 2026-05-19. NB: the
 * lucide-react `Sparkles` icon is unrelated and lives in lucide-react.
 */
export function Sparkles(_props: SparklesProps) {
  return null;
}
