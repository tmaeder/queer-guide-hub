// Stub: content-language badge. Renders nothing until language detection
// is wired up. Kept as a typed component so call sites don't break.

interface Props {
  text?: string | null;
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function ContentLangBadge(_props: Props) {
  return null;
}
