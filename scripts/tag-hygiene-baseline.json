{
  "_comment": "Ratchet baseline for scripts/check-tag-hygiene.mjs. Every value is a DEFECT COUNT that must not grow. Measured live on prod 2026-08-23 after Phase 1.1/1.3 applied and BEFORE Phase 2/3 (20260921100000, 20260921110000) apply — the ratchet fails on regression, never on improvement, so baselining the pre-migration state is safe and the numbers tighten with `--update` once they land. Never loosen a number to make CI pass.",

  "assignment_to_non_active_tag": 0,
  "commons_image_without_license": 4,
  "dangling_category_id": 0,
  "duplicate_active_name": 9,
  "image_alt_column_empty": 1204,
  "image_without_alt": 1204,
  "image_without_license": 1204,
  "indexable_without_description": 869,
  "nonclean_entity_type": 0,
  "redirect_to_non_canonical": 58,
  "sensitive_without_description": 19,
  "uncategorized_active": 17,

  "_notes": {
    "_transition": "image_without_alt and image_alt_column_empty are the SAME metric under two names. The old key is kept only until 20260921100000 lands, because the script iterates the keys the RPC actually returns and an unbaselined key is a hard failure — correct behaviour, but it would red every PR during the window where prod still runs the old function.",
    "duplicate_active_name": "9 is correct, not a backlog: the deliberate mat-* sense splits (mat-leather vs leather) excluded by 20260916110000. Growth means a real twin pair regrew.",
    "redirect_to_non_canonical": "301s into a 404. PR #2828 fixed this class once and it has regrown to 58; recorded so the 59th is caught, not so 58 is blessed.",
    "image_without_license": "1,193 of these are self-hosted (supabase.co, img.queer.guide) with no image_source, no image_attribution and no image_prompt — the provenance was never captured and CANNOT be recovered from the database, so this is a ratchet, not a target. See 20260921100000. Only commons_image_without_license is actionable.",
    "image_alt_column_empty": "NOT an accessibility gate. Every render path already emits alt=\"\" when this is null (TagIndexCard, MoreLikeThisByTag, FollowedTagsFeed hardcode it; TagDetail falls back to ''), which is correct for an image whose meaning is carried by the adjacent tag name.",
    "indexable_without_description": "869 thin public pages advertised in sitemap-tags.xml. 20260921110000 deindexes them and re-indexes on prose, after which this becomes a real zero-invariant rather than a backlog.",
    "sensitive_without_description": "Grows as the profession pipeline lands is_adult German profession tags with no description yet — not a regression introduced by the tag DQ program."
  }
}
